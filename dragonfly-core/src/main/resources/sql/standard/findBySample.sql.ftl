SELECT * FROM ${qualify(table)}<%assign columns=isSet(table.columns)/><%if columns?size &gt; 0> WHERE <%list columns as column>%{qualify(column)} = %{value[column.propertyName]}<%if column_has_next> AND </%if></%list></%if><#if metadata?size &gt; 0> ORDER BY <#list metadata as ordering>${qualify(ordering.column)} <#if ordering.order?string?lower_case == "asc">ASC<#else>DESC</#if><#if ordering_has_next>, </#if></#list></#if>;